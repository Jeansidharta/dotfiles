(deflisten current_window :initial "" "~/.config/eww/scripts/window-title-watcher/target/release/window-title-watcher")

(defwidget window_name [] 
  (scroll
    :hexpand true
    (box
      current_window)))
